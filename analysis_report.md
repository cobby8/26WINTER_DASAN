# 셔틀 중복 데이터 발생 원인 및 해결 방안 보고서

## 1. 문제 현상
*   배포 사이트에서 셔틀 데이터 동기화 후, 동일한 학생/시간대의 데이터가 2개씩 중복되어 보이는 현상 발생.
*   "새로 생성된 데이터"와 "기존 데이터"가 동시에 표시되고 있음.

## 2. 원인 분석
시스템 로그와 소스 코드를 분석한 결과, **데이터 조회 로직(View Logic)의 필터링 기준**이 원인으로 파악되었습니다.

1.  **동기화 과정 (Sync Process)**
    *   동기화 스크립트가 실행될 때, 기존 데이터와 엑셀 데이터 간에 미세한 차이(예: 초 단위 포맷 차이 등)가 감지되면 **"기존 데이터 삭제(Soft Delete) → 신규 데이터 생성(Insert)"** 로직이 수행됩니다.
    *   이 과정은 정상적인 데이터 갱신 절차입니다.

2.  **조회 과정 (Display Logic)**
    *   현재 `getDailyShuttleData` 함수는 과거 히스토리 보존을 위해 **"날짜 시작 시점(00:00)"**을 기준으로 유효성을 판단합니다.
    *   **문제점:** 오늘 날짜(`2026-02-04`)에 삭제된 데이터는 "오늘 시작 시점(`2026-02-04 00:00`)에는 유효했으므로 보여준다"는 로직이 적용되어 있습니다.
    *   이로 인해 **"오늘 삭제된 구 데이터"**와 **"오늘 생성된 신규 데이터"**가 동시에 화면에 노출되어 중복처럼 보이게 됩니다.

## 3. 해결 방안
*   **조회 필터 수정:** 해당 날짜에 대해 데이터를 조회할 때, **"그 날짜 종료 시점(23:59)까지 살아있는 데이터"** 만 보여주도록 조회 기준을 강화합니다.
*   이렇게 하면 오늘 삭제된 데이터는 목록에서 숨겨지고, 최종적으로 유효한(Active) 데이터 1건만 표시됩니다.

## 4. 로컬 vs 배포 불일치 원인 분석 (2026-02-04 추가)
*   **현상:** 로컬 환경에서는 정상(44명/중복없음)이나, 배포 환경에서는 여전히 중복(75명)으로 표시됨.
*   **원인:**
    *   **로컬 환경:** 제가 적용한 "조회 필터 수정 코드"가 즉시 반영되어, 삭제된 데이터가 정상적으로 숨겨졌습니다.
    *   **배포 환경:** Git Push 이후 Vercel 등에서 빌드 및 배포가 완료되기 전까지는 "수정 전 코드"가 작동하므로, 삭제된 데이터까지 모두 보여주고 있습니다.
*   **결론:** 로컬 환경의 결과가 **"수정 후 정상 동작"** 모습입니다. 배포가 완료되면 로컬과 동일하게 중복이 사라질 것입니다.

## 5. 조치 계획
*   `src/app/actions/shuttle-ops-actions.ts` 파일의 조회 필터 조건을 수정하여 배포합니다. (완료)
*   배포 완료 대기 후 재확인 필요.
